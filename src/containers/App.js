import React, { Component } from 'react';
import MainPage from './MainPage';

class App extends Component {
    render() {
        return (
            <div id="app">
                <MainPage />
            </div>
        )
    }
}

export default App;